# app/ (placeholder)

Mobile app scaffolding is intentionally deferred. Per CLAUDE.md §7:

> Confirm the Flutter-vs-React-Native choice with the user before scaffolding `/app`.

Once confirmed, this directory becomes the Flutter or React Native project root.
